import {calculateScoreAction,generateApproachAction} from "./actions";
type Research={summary:string|null;opportunityReason:string|null;hypotheticalPain:string|null;recommendedService:string|null};
export function IntelligencePanel({leadId,factors,messages,research}:{leadId:string;factors:Array<{id:string;explanation:string;contribution:number}>;messages:Array<{id:string;channel:string;subject:string|null;body:string;status:string;createdAt:string}>;research:Research|null}){return <section className="space-y-5 rounded-2xl border border-slate-200 bg-white p-5"><div className="flex flex-wrap items-center justify-between gap-3"><div><h2 className="font-bold">VIO Sales Intelligence</h2><p className="text-xs text-slate-500">Análise explicável com os dados públicos registrados.</p></div><form action={calculateScoreAction.bind(null,leadId)}><button className="rounded-xl bg-slate-950 px-4 py-2 text-xs font-bold text-white">Calcular análise e score</button></form></div>{research?<div className="grid gap-3 md:grid-cols-2"><Info title="Resumo" text={research.summary}/><Info title="Por que é interessante" text={research.opportunityReason}/><Info title="Possível dor — hipótese" text={research.hypotheticalPain}/><Info title="Serviço recomendado" text={research.recommendedService}/></div>:<p className="rounded-xl bg-slate-50 p-4 text-sm text-slate-500">Calcule para gerar a primeira análise. Nenhuma dificuldade financeira será presumida.</p>}{factors.length>0&&<div><h3 className="text-sm font-bold">Motivos do score</h3><ul className="mt-2 space-y-2">{factors.map(f=><li className="text-sm text-slate-600" key={f.id}>+{f.contribution} · {f.explanation}</li>)}</ul></div>}<div className="border-t border-slate-100 pt-4"><h3 className="text-sm font-bold">Gerar abordagem</h3><form action={generateApproachAction.bind(null,leadId)} className="mt-3 flex flex-wrap gap-2"><select className="rounded-xl border px-3 py-2 text-sm" name="channel"><option value="EMAIL">E-mail</option><option value="WHATSAPP">WhatsApp</option><option value="LINKEDIN">LinkedIn</option></select><select className="rounded-xl border px-3 py-2 text-sm" name="tone"><option value="CONSULTATIVE">Consultiva</option><option value="EXECUTIVE">Executiva</option><option value="DIRECT">Direta</option><option value="PROFESSIONAL_INFORMAL">Informal profissional</option><option value="SHORT">Curta</option></select><button className="rounded-xl bg-blue-600 text-white px-4 py-2 text-sm font-bold">Gerar</button></form></div>{messages.length>0&&<div className="space-y-3">{messages.map(m=><article className="rounded-xl border p-4" key={m.id}><p className="text-xs font-bold text-blue-600">{m.channel} · {m.status}</p>{m.subject&&<p className="mt-2 font-bold">{m.subject}</p>}<p className="mt-2 whitespace-pre-wrap text-sm leading-6">{m.body}</p></article>)}</div>}</section>}
function Info({title,text}:{title:string;text:string|null}){return <div className="rounded-xl bg-slate-50 p-4"><p className="text-xs font-bold uppercase text-slate-400">{title}</p><p className="mt-2 text-sm leading-6 text-slate-700">{text??"Não identificado publicamente."}</p></div>}
